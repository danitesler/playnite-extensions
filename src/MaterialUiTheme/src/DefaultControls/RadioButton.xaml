<!--
    Overlay on Playnite Default/DefaultControls/RadioButton.xaml.
    MUI Radio: 2px text.secondary ring; checked = primary ring with a primary dot. Round hover state layer.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="{x:Type RadioButton}" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource MuiTextPrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource MuiTextSecondaryBrush}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource MuiFocusVisible}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RadioButton}">
                    <BulletDecorator Background="Transparent">
                        <BulletDecorator.Bullet>
                            <Grid Width="18" Height="18" VerticalAlignment="Center">
                                <Ellipse x:Name="StateLayer" Margin="-10" Visibility="Hidden" IsHitTestVisible="False"
                                         Fill="{DynamicResource MuiActionHoverBrush}" />
                                <Ellipse x:Name="Ring" StrokeThickness="2" Stroke="{TemplateBinding BorderBrush}" />
                                <Ellipse x:Name="Dot" Width="8" Height="8" Visibility="Collapsed"
                                         Fill="{DynamicResource MuiPrimaryMainBrush}" />
                            </Grid>
                        </BulletDecorator.Bullet>
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter x:Name="Label" RecognizesAccessKey="True" Margin="10,0,0,0"
                                              VerticalAlignment="Center" HorizontalAlignment="Left" />
                        </Border>
                    </BulletDecorator>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasContent" Value="False">
                            <Setter Property="Margin" Value="0" TargetName="Label" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Visibility" Value="Visible" TargetName="StateLayer" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Visibility" Value="Visible" TargetName="Dot" />
                            <Setter Property="Stroke" Value="{DynamicResource MuiPrimaryMainBrush}" TargetName="Ring" />
                            <Setter Property="Fill" Value="{DynamicResource MuiPrimaryHoverBrush}" TargetName="StateLayer" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
