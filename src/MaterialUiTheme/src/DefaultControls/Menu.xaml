<!--
    Overlay on Playnite Default/DefaultControls/Menu.xaml.
    MUI MenuItem: full-width and square, 6px 16px (MuiMenuItemPadding), text.primary, action.hover on hover; a
    ListItemIcon column (36px minimum) for icons and the check of checked items (Material check icon, 20px); the
    shortcut as body2 in text.secondary; a chevron_right on items that open a nested menu, which opens on the same
    Menu paper. Dividers are 1px divider lines with 8px above and below. Playnite's MenuItem style is kept; it picks
    these templates up by key.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="{x:Type Separator}">
        <Setter Property="Background" Value="{DynamicResource MuiDividerBrush}" />
        <Setter Property="Margin" Value="0,8,0,8" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Separator}">
                    <Border BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Background="{TemplateBinding Background}"
                            Height="1" SnapsToDevicePixels="True" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="{x:Type Separator}" BasedOn="{StaticResource {x:Type Separator}}" />

    <!-- TopLevelHeader -->
    <ControlTemplate x:Key="{x:Static MenuItem.TopLevelHeaderTemplateKey}" TargetType="{x:Type MenuItem}">
        <Border x:Name="Border">
            <Grid>
                <Border x:Name="HoverBorder" Background="{DynamicResource MuiActionHoverBrush}" Visibility="Collapsed"
                        CornerRadius="0" />
                <TextBlock VerticalAlignment="Center" Padding="{DynamicResource MuiMenuItemPadding}" Style="{DynamicResource BaseTextBlockStyle}"
                           Foreground="{TemplateBinding Foreground}">
                    <ContentPresenter ContentSource="Header" RecognizesAccessKey="True" />
                </TextBlock>
                <Popup Placement="Bottom" VerticalOffset="4" IsOpen="{TemplateBinding IsSubmenuOpen}"
                       AllowsTransparency="True" Focusable="False">
                    <Border SnapsToDevicePixels="True" Padding="{DynamicResource MuiMenuListPadding}"
                            BorderBrush="{DynamicResource PopupBorderBrush}"
                            Background="{DynamicResource PopupBackgroundBrush}"
                            BorderThickness="{DynamicResource PopupBorderThickness}"
                            CornerRadius="{DynamicResource MuiShapeBorderRadius}">
                        <StackPanel Grid.IsSharedSizeScope="True" IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle" />
                    </Border>
                </Popup>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Visibility" Value="Visible" TargetName="HoverBorder" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- TopLevelItem -->
    <ControlTemplate x:Key="{x:Static MenuItem.TopLevelItemTemplateKey}" TargetType="{x:Type MenuItem}">
        <Border x:Name="Border" Padding="{DynamicResource MuiMenuItemPadding}" CornerRadius="0" Background="Transparent">
            <ContentPresenter ContentSource="Header" RecognizesAccessKey="True" />
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{DynamicResource MuiActionHoverBrush}" TargetName="Border" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- SubmenuHeader -->
    <ControlTemplate x:Key="{x:Static MenuItem.SubmenuHeaderTemplateKey}" TargetType="{x:Type MenuItem}">
        <Grid Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" SharedSizeGroup="MenuItemIconColumnGroup" />
                <ColumnDefinition Width="*" SharedSizeGroup="MenuItemIGTColumnGroup" />
                <ColumnDefinition Width="Auto" SharedSizeGroup="MenuItemShortcutColumnGroup" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Border x:Name="HoverBorder" Grid.Column="0" Grid.ColumnSpan="4" Visibility="Collapsed"
                    Background="{DynamicResource MuiActionHoverBrush}" CornerRadius="0" />
            <ContentPresenter x:Name="Icon" Grid.Column="0" Margin="16,0,4,0"
                              MaxHeight="16" MaxWidth="16"
                              VerticalAlignment="Center" ContentSource="Icon" />
            <ContentPresenter x:Name="GlyphPanel" Grid.Column="0" Visibility="Collapsed" Margin="16,0,0,0"
                              Width="20" Height="20" VerticalAlignment="Center"
                              Content="{DynamicResource MuiIconCheck}"
                              ContentTemplate="{DynamicResource MuiIconTemplate}" />
            <TextBlock Grid.Column="1" Padding="{DynamicResource MuiMenuItemPadding}" VerticalAlignment="Center" Style="{DynamicResource BaseTextBlockStyle}"
                       Foreground="{TemplateBinding Foreground}">
                <ContentPresenter RecognizesAccessKey="True" ContentSource="Header" />
            </TextBlock>
            <TextBlock x:Name="GestureText" Grid.Column="2" Text="{TemplateBinding InputGestureText}"
                       TextAlignment="Left" HorizontalAlignment="Stretch" Margin="16,5,4,5"
                       VerticalAlignment="Center" Background="Transparent"
                       FontSize="{DynamicResource FontSize}"
                       Foreground="{DynamicResource MuiTextSecondaryBrush}" />
            <ContentPresenter Grid.Column="3" Margin="4,0,8,0" Width="20" Height="20" VerticalAlignment="Center"
                              TextElement.Foreground="{DynamicResource MuiActionActiveBrush}"
                              Content="{DynamicResource MuiIconChevronRight}"
                              ContentTemplate="{DynamicResource MuiIconTemplate}" />
            <Popup IsOpen="{Binding Path=IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}" Placement="Right"
                   HorizontalOffset="8" VerticalOffset="-5" Focusable="False" AllowsTransparency="True">
                <Border SnapsToDevicePixels="True" Padding="{DynamicResource MuiMenuListPadding}"
                        BorderBrush="{DynamicResource PopupBorderBrush}"
                        Background="{DynamicResource PopupBackgroundBrush}"
                        BorderThickness="{DynamicResource PopupBorderThickness}"
                        CornerRadius="{DynamicResource MuiShapeBorderRadius}">
                    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                        <StackPanel Grid.IsSharedSizeScope="True" IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle" />
                    </ScrollViewer>
                </Border>
            </Popup>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="Icon" Value="{x:Null}">
                <Setter Property="Visibility" Value="Collapsed" TargetName="Icon" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Visibility" Value="Visible" TargetName="GlyphPanel" />
                <Setter Property="Visibility" Value="Collapsed" TargetName="Icon" />
            </Trigger>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Visibility" Value="Visible" TargetName="HoverBorder" />
            </Trigger>
            <Trigger Property="IsSubmenuOpen" Value="True">
                <Setter Property="Visibility" Value="Visible" TargetName="HoverBorder" />
            </Trigger>
            <Trigger Property="InputGestureText" Value="">
                <Setter Property="Visibility" Value="Collapsed" TargetName="GestureText" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- SubmenuItem -->
    <ControlTemplate x:Key="{x:Static MenuItem.SubmenuItemTemplateKey}" TargetType="{x:Type MenuItem}">
        <Border x:Name="HoverBorder" Background="Transparent" CornerRadius="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="MenuItemIconColumnGroup" />
                    <ColumnDefinition Width="*" SharedSizeGroup="MenuItemIGTColumnGroup" />
                    <ColumnDefinition Width="Auto" SharedSizeGroup="MenuItemShortcutColumnGroup" />
                </Grid.ColumnDefinitions>
                <ContentPresenter x:Name="Icon" Grid.Column="0" Margin="16,0,4,0"
                                  MaxHeight="16" MaxWidth="16"
                                  VerticalAlignment="Center" ContentSource="Icon" />
                <ContentPresenter x:Name="GlyphPanel" Grid.Column="0" Visibility="Collapsed" Margin="16,0,0,0"
                                  Width="20" Height="20" VerticalAlignment="Center"
                                  Content="{DynamicResource MuiIconCheck}"
                                  ContentTemplate="{DynamicResource MuiIconTemplate}" />
                <TextBlock VerticalAlignment="Center" Grid.Column="1" Padding="{DynamicResource MuiMenuItemPadding}" Style="{DynamicResource BaseTextBlockStyle}"
                           Foreground="{TemplateBinding Foreground}">
                    <ContentPresenter RecognizesAccessKey="True" ContentSource="Header" />
                </TextBlock>
                <TextBlock x:Name="GestureText" Grid.Column="2" Text="{TemplateBinding InputGestureText}"
                           TextAlignment="Left" HorizontalAlignment="Stretch"
                           VerticalAlignment="Center" Margin="16,5,8,5"
                           FontSize="{DynamicResource FontSize}"
                           Foreground="{DynamicResource MuiTextSecondaryBrush}" />
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="Icon" Value="{x:Null}">
                <Setter Property="Visibility" Value="Collapsed" TargetName="Icon" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Visibility" Value="Visible" TargetName="GlyphPanel" />
                <Setter Property="Visibility" Value="Collapsed" TargetName="Icon" />
            </Trigger>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{DynamicResource MuiActionHoverBrush}" TargetName="HoverBorder" />
            </Trigger>
            <Trigger Property="InputGestureText" Value="">
                <Setter Property="Visibility" Value="Collapsed" TargetName="GestureText" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
</ResourceDictionary>
