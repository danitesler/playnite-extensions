<!--
    Overlay on Playnite Default/DefaultControls/GroupBox.xaml.
    MUI Card: Paper at elevation 1, shape.borderRadius, no border; CardHeader and CardContent padding 16
    (MuiCardContentPadding, MuiCardHeaderMargin). The title is Typography h6 (20px, fontWeightMedium) rather than
    CardHeader's default h5, which is sized for standalone cards, not settings sections.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="{x:Type GroupBox}" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Foreground" Value="{DynamicResource MuiTextPrimaryBrush}" />
        <Setter Property="Background" Value="{DynamicResource MuiPaperElevation1Brush}" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="{DynamicResource MuiCardContentPadding}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type GroupBox}">
                    <Border SnapsToDevicePixels="True"
                            Background="{TemplateBinding Background}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="{DynamicResource MuiShapeBorderRadius}">
                        <DockPanel>
                            <TextBlock x:Name="HeaderText" DockPanel.Dock="Top" Style="{DynamicResource BaseTextBlockStyle}"
                                       Foreground="{TemplateBinding Foreground}" FontWeight="Medium"
                                       FontSize="{DynamicResource FontSizeLarger}" Margin="{DynamicResource MuiCardHeaderMargin}">
                                <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" ContentSource="Header"
                                                  RecognizesAccessKey="True" />
                            </TextBlock>
                            <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" DockPanel.Dock="Top" />
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasHeader" Value="False">
                            <Setter Property="Visibility" Value="Collapsed" TargetName="HeaderText" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
