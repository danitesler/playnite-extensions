<!--
    Overlay on Playnite Default/DefaultControls/RepeatButton.xaml.
    MUI Button, variant="outlined", medium: transparent fill, a 1px primary edge at 50% (alpha(primary.main, 0.5)),
    primary label, alpha(primary.main, hoverOpacity) on hover, shape.borderRadius, 5px 15px.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="{x:Type RepeatButton}" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource MuiPrimaryMainBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource MuiPrimaryOutlinedBorderBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource ControlBorderThickness}" />
        <Setter Property="Padding" Value="15,5,15,5" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border SnapsToDevicePixels="True"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{DynamicResource MuiShapeBorderRadius}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource MuiPrimaryHoverBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource MuiPrimaryMainBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource MuiPrimarySelectedBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
