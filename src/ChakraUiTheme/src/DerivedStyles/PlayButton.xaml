<!--
    Overlay on Playnite Default/DerivedStyles/PlayButton.xaml.
    Chakra Button, "solid" variant in the brand palette, size md: colorPalette.solid fill, colorPalette.contrast text,
    colorPalette.solid/90 on hover, radius l2, fontWeight medium, h-10 / px-4 (ChakraButtonPadding).
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="PlayButton" TargetType="{x:Type Button}" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="Background" Value="{DynamicResource ChakraColorPaletteSolidBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource ChakraColorPaletteContrastBrush}" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="{DynamicResource ControlBorderThickness}" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="Padding" Value="{DynamicResource ChakraButtonPadding}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource ChakraFocusRing}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid x:Name="Grid">
                        <Border x:Name="DefaultBorder" SnapsToDevicePixels="True"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="{DynamicResource ChakraRadiiL2}" />
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource ChakraColorPaletteSolid90Brush}" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
