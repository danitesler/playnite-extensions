<!--
    Overlay on Playnite Default/DefaultControls/CheckBox.xaml.
    Chakra Checkbox, "solid" variant, size md: a 20px (boxSize 5) control with p-0.5, radius l1, and a 1px
    border.emphasized edge; checked and indeterminate fill with colorPalette.solid and draw the mark in
    colorPalette.contrast. The label sits 10px away (gap 2.5) at fontWeight medium. Keyboard focus is Chakra's outside
    ring (ChakraFocusRing).
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="{x:Type CheckBox}" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource ChakraFgBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ChakraBorderEmphasizedBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource ControlBorderThickness}" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource ChakraFocusRing}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CheckBox}">
                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                        <DockPanel>
                            <Border x:Name="Control" DockPanel.Dock="Left" Width="20" Height="20" Padding="2"
                                    VerticalAlignment="Center" SnapsToDevicePixels="True"
                                    CornerRadius="{DynamicResource ChakraRadiiL1}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    Background="{DynamicResource CheckBoxCheckMarkBkBrush}">
                                <Grid TextElement.Foreground="{DynamicResource ChakraColorPaletteContrastBrush}">
                                    <ContentPresenter x:Name="CheckIcon" Visibility="Collapsed"
                                                      Content="{DynamicResource ChakraIconCheck}"
                                                      ContentTemplate="{DynamicResource ChakraIconTemplate}" />
                                    <Rectangle x:Name="IndeterminateIcon" Visibility="Collapsed" Width="10" Height="2"
                                               RadiusX="1" RadiusY="1"
                                               Fill="{DynamicResource ChakraColorPaletteContrastBrush}" />
                                </Grid>
                            </Border>
                            <ContentPresenter x:Name="Label" DockPanel.Dock="Left" RecognizesAccessKey="True" Margin="10,0,0,0"
                                              VerticalAlignment="Center" HorizontalAlignment="Left" />
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasContent" Value="False">
                            <Setter Property="Margin" Value="0" TargetName="Label" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="{x:Null}" />
                                <Condition Property="IsThreeState" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible" TargetName="IndeterminateIcon" />
                            <Setter Property="Background" Value="{DynamicResource ChakraColorPaletteSolidBrush}" TargetName="Control" />
                            <Setter Property="BorderBrush" Value="{DynamicResource ChakraColorPaletteSolidBrush}" TargetName="Control" />
                        </MultiTrigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Visibility" Value="Visible" TargetName="CheckIcon" />
                            <Setter Property="Background" Value="{DynamicResource ChakraColorPaletteSolidBrush}" TargetName="Control" />
                            <Setter Property="BorderBrush" Value="{DynamicResource ChakraColorPaletteSolidBrush}" TargetName="Control" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
