<!--
    Overlay on Playnite Default/DefaultControls/ComboBox.xaml.
    Fluent Dropdown, outline, medium (react-combobox): colorNeutralBackground1 fill, 1px colorNeutralStroke1 edge with
    a colorNeutralStrokeAccessible bottom (StrokeAccessibleHover under the pointer), borderRadiusMedium, 32px,
    chevron_down_20_regular in colorNeutralStrokeAccessible. Keyboard focus grows a 2px colorCompoundBrandStroke
    underline from the center (0.2s in, 0.1s out). The listbox is a popover like the MenuPopover
    (colorNeutralBackground1, borderRadiusMedium, 4px padding) with Options: 6px 8px, borderRadiusMedium,
    colorNeutralBackground1Hover under the pointer, and checkmark_16_filled before the selected option's label.
    Part names (PART_EditableTextBox, PART_Popup) are Playnite's.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="{x:Type ComboBox}" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="Foreground" Value="{DynamicResource FluentNeutralForeground1Brush}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Background" Value="{DynamicResource FluentNeutralBackground1Brush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource FluentNeutralStroke1Brush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource ControlBorderThickness}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource FluentFocusOutline}" />
        <Setter Property="Padding" Value="{DynamicResource FluentInputPadding}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <Grid>
                        <Border x:Name="Border" SnapsToDevicePixels="True"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{DynamicResource FluentBorderRadiusMedium}">
                            <Grid>
                                <ToggleButton x:Name="ToggleButton" Focusable="False" ClickMode="Press"
                                              IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                                            <DockPanel LastChildFill="False" Background="Transparent">
                                                <ContentPresenter DockPanel.Dock="Right" Margin="0,0,10,0" Width="20" Height="20"
                                                                  VerticalAlignment="Center"
                                                                  TextElement.Foreground="{DynamicResource FluentNeutralStrokeAccessibleBrush}"
                                                                  Content="{DynamicResource FluentIconChevronDown}"
                                                                  ContentTemplate="{DynamicResource FluentIconTemplate}" />
                                            </DockPanel>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <!-- Content inset by Padding (FluentInputPadding) plus room for the 20px chevron. -->
                                <Border Margin="0,0,26,0" Padding="{TemplateBinding Padding}" IsHitTestVisible="False">
                                    <ContentPresenter x:Name="ContentSite" HorizontalAlignment="Left" VerticalAlignment="Center"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" />
                                </Border>
                                <Border Margin="0,0,26,0" Padding="{TemplateBinding Padding}">
                                    <TextBox x:Name="PART_EditableTextBox" Visibility="Hidden" VerticalAlignment="Center"
                                             HorizontalAlignment="Stretch" Focusable="True"
                                             Style="{DynamicResource FluentBareTextBox}"
                                             IsReadOnly="{TemplateBinding IsReadOnly}" />
                                </Border>
                                <Popup x:Name="PART_Popup" IsOpen="{TemplateBinding IsDropDownOpen}"
                                       Placement="Bottom" VerticalOffset="4" Focusable="False" AllowsTransparency="True">
                                    <Grid MaxHeight="{TemplateBinding MaxDropDownHeight}" MinWidth="{TemplateBinding ActualWidth}"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                                        <Border BorderBrush="{DynamicResource PopupBorderBrush}"
                                                BorderThickness="{DynamicResource PopupBorderThickness}"
                                                Background="{DynamicResource PopupBackgroundBrush}"
                                                CornerRadius="{DynamicResource FluentBorderRadiusMedium}" Padding="{DynamicResource FluentMenuPopoverPadding}">
                                            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                                                <ItemsPresenter Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained"
                                                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                            </ScrollViewer>
                                        </Border>
                                    </Grid>
                                </Popup>
                            </Grid>
                        </Border>
                        <Border x:Name="Underline" IsHitTestVisible="False" SnapsToDevicePixels="True"
                                BorderThickness="0,0,0,1" CornerRadius="{DynamicResource FluentBorderRadiusMedium}"
                                BorderBrush="{DynamicResource FluentNeutralStrokeAccessibleBrush}" />
                        <Border x:Name="FocusLine" IsHitTestVisible="False" SnapsToDevicePixels="True"
                                BorderThickness="0,0,0,2" CornerRadius="{DynamicResource FluentBorderRadiusMedium}"
                                BorderBrush="{DynamicResource FluentCompoundBrandStrokeBrush}" RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="0" />
                            </Border.RenderTransform>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" Value="{DynamicResource FluentNeutralStrokeAccessibleHoverBrush}" TargetName="Underline" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="FocusLine" To="1" Duration="0:0:0.2"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="FocusLine" To="0" Duration="0:0:0.1"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter Property="IsTabStop" Value="False" />
                            <Setter Property="Visibility" Value="Visible" TargetName="PART_EditableTextBox" />
                            <Setter Property="Visibility" Value="Hidden" TargetName="ContentSite" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ComboBoxItem}">
        <Setter Property="Foreground" Value="{DynamicResource FluentNeutralForeground1Brush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Padding" Value="{DynamicResource FluentOptionPadding}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <Border x:Name="ItemBorder" Background="{TemplateBinding Background}"
                            CornerRadius="{DynamicResource FluentBorderRadiusMedium}" Padding="{TemplateBinding Padding}">
                        <DockPanel>
                            <!-- Option checkIcon: 16px, spacingHorizontalXS (4px) before the label. -->
                            <ContentPresenter x:Name="CheckIcon" DockPanel.Dock="Left" Visibility="Hidden"
                                              Width="16" Height="16" Margin="0,0,4,0" VerticalAlignment="Center"
                                              Content="{DynamicResource FluentIconCheckmark16}"
                                              ContentTemplate="{DynamicResource FluentIcon16Template}" />
                            <ContentPresenter VerticalAlignment="Center" />
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Visibility" Value="Visible" TargetName="CheckIcon" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource FluentNeutralBackground1HoverBrush}" TargetName="ItemBorder" />
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter Property="Background" Value="{DynamicResource FluentNeutralBackground1HoverBrush}" TargetName="ItemBorder" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
