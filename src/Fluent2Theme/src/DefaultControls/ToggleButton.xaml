<!--
    Overlay on Playnite Default/DefaultControls/ToggleButton.xaml.
    Fluent ToggleButton (react-button), secondary appearance: the Button chrome (colorNeutralBackground1,
    colorNeutralStroke1 edge, borderRadiusMedium, FluentButtonPadding), colorNeutralBackground1Hover on hover and
    colorNeutralBackground1Selected when checked.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="{x:Type ToggleButton}" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="Background" Value="{DynamicResource FluentNeutralBackground1Brush}" />
        <Setter Property="Foreground" Value="{DynamicResource FluentNeutralForeground1Brush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource FluentNeutralStroke1Brush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource ControlBorderThickness}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Padding" Value="{DynamicResource FluentButtonPadding}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource FluentFocusOutline}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border x:Name="Border" SnapsToDevicePixels="True"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{DynamicResource FluentBorderRadiusMedium}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource FluentNeutralBackground1HoverBrush}" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Background" Value="{DynamicResource FluentNeutralBackground1SelectedBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource FluentNeutralForeground1Brush}" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
