<!--
    Fluent kit override of Shadcn/Desktop/DefaultControls/TabControl.xaml.
    Fluent TabList: no rail; tabs in colorNeutralForeground2 with a subtle hover fill; a 3px rounded indicator
    inset by the tab padding shows colorNeutralStroke1Hover on hover and colorCompoundBrandStroke when selected,
    where the label turns Foreground1 semibold.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="{x:Type TabControl}" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabControl}">
                    <Grid KeyboardNavigation.TabNavigation="Local">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition x:Name="ColumnDefinition0" />
                            <ColumnDefinition x:Name="ColumnDefinition1" Width="0" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" x:Name="RowDefinition0" />
                            <RowDefinition Height="*" x:Name="RowDefinition1" />
                        </Grid.RowDefinitions>
                        <Border x:Name="HeaderBorder" Margin="0,0,0,8" SnapsToDevicePixels="True"
                                Background="Transparent" BorderThickness="0"
                                BorderBrush="{DynamicResource ShadcnBorderBrush}">
                            <TabPanel x:Name="HeaderPanel" IsItemsHost="True" Panel.ZIndex="1"
                                      KeyboardNavigation.TabIndex="1" />
                        </Border>
                        <Border x:Name="ContentPanel" Grid.Column="0" Grid.Row="1"
                                KeyboardNavigation.DirectionalNavigation="Contained" KeyboardNavigation.TabIndex="2"
                                KeyboardNavigation.TabNavigation="Local" Background="{DynamicResource ControlBackgroundBrush}">
                            <ContentPresenter x:Name="PART_SelectedContentHost" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                              ContentSource="SelectedContent" />
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="TabStripPlacement" Value="Bottom">
                            <Setter Property="Grid.Row" TargetName="ContentPanel" Value="0" />
                            <Setter Property="Height" TargetName="RowDefinition0" Value="*" />
                            <Setter Property="Height" TargetName="RowDefinition1" Value="Auto" />
                            <Setter Property="Grid.Row" TargetName="HeaderBorder" Value="1" />
                            <Setter Property="Margin" TargetName="HeaderBorder" Value="0,8,0,0" />
                        </Trigger>
                        <Trigger Property="TabStripPlacement" Value="Left">
                            <Setter Property="Grid.Row" TargetName="ContentPanel" Value="0" />
                            <Setter Property="Grid.Column" TargetName="ContentPanel" Value="1" />
                            <Setter Property="Width" TargetName="ColumnDefinition0" Value="Auto" />
                            <Setter Property="Width" TargetName="ColumnDefinition1" Value="*" />
                            <Setter Property="Height" TargetName="RowDefinition0" Value="*" />
                            <Setter Property="Height" TargetName="RowDefinition1" Value="0" />
                            <Setter Property="Grid.Column" TargetName="HeaderBorder" Value="0" />
                            <Setter Property="Margin" TargetName="HeaderBorder" Value="0,0,8,0" />
                        </Trigger>
                        <Trigger Property="TabStripPlacement" Value="Right">
                            <Setter Property="Grid.Row" TargetName="ContentPanel" Value="0" />
                            <Setter Property="Grid.Column" TargetName="ContentPanel" Value="0" />
                            <Setter Property="Width" TargetName="ColumnDefinition0" Value="*" />
                            <Setter Property="Width" TargetName="ColumnDefinition1" Value="Auto" />
                            <Setter Property="Height" TargetName="RowDefinition0" Value="*" />
                            <Setter Property="Height" TargetName="RowDefinition1" Value="0" />
                            <Setter Property="Grid.Column" TargetName="HeaderBorder" Value="1" />
                            <Setter Property="Margin" TargetName="HeaderBorder" Value="8,0,0,0" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type TabItem}" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Foreground" Value="{DynamicResource ShadcnMutedForegroundBrush}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource ShadcnFocusVisual}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabItem}">
                    <Border x:Name="TabBorder" Background="Transparent" CornerRadius="{DynamicResource ShadcnRadiusMd}">
                        <Grid>
                            <TextBlock x:Name="TextContent" Margin="10,10,10,12" Style="{DynamicResource BaseTextBlockStyle}"
                                       Foreground="{TemplateBinding Foreground}">
                                <ContentPresenter x:Name="ContentSite" RecognizesAccessKey="True" ContentSource="Header"
                                                  HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </TextBlock>
                            <!-- selectedIndicator: strokeWidthThicker, borderRadiusCircular, inset by the tab padding. -->
                            <Border x:Name="Indicator" Height="3" Margin="10,0,10,0" VerticalAlignment="Bottom"
                                    IsHitTestVisible="False" SnapsToDevicePixels="True" Background="Transparent"
                                    CornerRadius="{DynamicResource ShadcnRadiusFull}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="TabStripPlacement" Value="Bottom">
                            <Setter Property="VerticalAlignment" Value="Top" TargetName="Indicator" />
                        </Trigger>
                        <Trigger Property="TabStripPlacement" Value="Left">
                            <Setter Property="Height" Value="Auto" TargetName="Indicator" />
                            <Setter Property="Width" Value="3" TargetName="Indicator" />
                            <Setter Property="Margin" Value="0,8,0,8" TargetName="Indicator" />
                            <Setter Property="VerticalAlignment" Value="Stretch" TargetName="Indicator" />
                            <Setter Property="HorizontalAlignment" Value="Left" TargetName="Indicator" />
                        </Trigger>
                        <Trigger Property="TabStripPlacement" Value="Right">
                            <Setter Property="Height" Value="Auto" TargetName="Indicator" />
                            <Setter Property="Width" Value="3" TargetName="Indicator" />
                            <Setter Property="Margin" Value="0,8,0,8" TargetName="Indicator" />
                            <Setter Property="VerticalAlignment" Value="Stretch" TargetName="Indicator" />
                            <Setter Property="HorizontalAlignment" Value="Right" TargetName="Indicator" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource ShadcnAccentBrush}" TargetName="TabBorder" />
                            <Setter Property="Background" Value="{DynamicResource ShadcnInputBrush}" TargetName="Indicator" />
                            <Setter Property="Foreground" Value="{DynamicResource ShadcnForegroundBrush}" TargetName="TextContent" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Panel.ZIndex" Value="1" />
                            <Setter Property="Background" Value="{DynamicResource ShadcnTabIndicatorBrush}" TargetName="Indicator" />
                            <Setter Property="Foreground" Value="{DynamicResource ShadcnForegroundBrush}" TargetName="TextContent" />
                            <Setter Property="FontWeight" Value="SemiBold" TargetName="TextContent" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
