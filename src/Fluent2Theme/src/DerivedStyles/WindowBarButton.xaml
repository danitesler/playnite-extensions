<!--
    Overlay on Playnite Default/DerivedStyles/WindowBarButton.xaml (dialog title bars; the main window has its own
    FluentCaptionButton in DerivedStyles/MainWindowStyle.xaml).
    Windows 11 caption buttons drawn with Fluent UI System Icons (Media.xaml) instead of Marlett: square, no edge,
    colorSubtleBackgroundHover on hover, and colorPaletteRedBackground3 behind the close button. Playnite's window
    templates set Content to the Marlett character ("0" minimize, "1" maximize, "2" restore, "r" close); the
    template maps it to an icon. The size stays Playnite's (the dialog title band is 25px).
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="WindowBarButton" TargetType="Button">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource FluentNeutralForeground3Brush}" />
        <Setter Property="Width" Value="30" />
        <Setter Property="Height" Value="22" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Margin" Value="2,0,2,0" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}" CornerRadius="{DynamicResource FluentBorderRadiusMedium}">
                        <!-- FontSize carries the icon size. -->
                        <ContentPresenter x:Name="Glyph"
                                          Width="{TemplateBinding FontSize}" Height="{TemplateBinding FontSize}"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Content="{DynamicResource FluentIconWindowClose}"
                                          ContentTemplate="{DynamicResource FluentIconTemplate}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Content" Value="0">
                            <Setter Property="Content" Value="{DynamicResource FluentIconWindowMinimize}" TargetName="Glyph" />
                        </Trigger>
                        <Trigger Property="Content" Value="1">
                            <Setter Property="Content" Value="{DynamicResource FluentIconWindowMaximize}" TargetName="Glyph" />
                        </Trigger>
                        <Trigger Property="Content" Value="2">
                            <Setter Property="Content" Value="{DynamicResource FluentIconWindowRestore}" TargetName="Glyph" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="{DynamicResource FluentNeutralForeground1Brush}" />
                <Setter Property="Background" Value="{DynamicResource FluentSubtleBackgroundHoverBrush}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="Content" Value="r" />
                </MultiTrigger.Conditions>
                <Setter Property="Foreground" Value="{DynamicResource FluentNeutralForeground1Brush}" />
                <Setter Property="Background" Value="{DynamicResource FluentPaletteRedBackground3Brush}" />
            </MultiTrigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
