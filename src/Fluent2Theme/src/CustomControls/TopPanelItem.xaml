<!--
    Overlay on Playnite Default/CustomControls/TopPanelItem.xaml.
    Fluent Button, subtle appearance, icon only, medium: 32px, borderRadiusMedium, 20px icons. Hover fills with
    colorSubtleBackgroundHover and turns the icon brand (colorNeutralForeground2BrandHover); a toggled item (current
    view, open explorer) keeps both, like a checked subtle ToggleButton. Plugin glyphs get FontSize 20.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="{x:Type TopPanelItem}">
        <Setter Property="MinWidth" Value="32" />
        <Setter Property="Height" Value="32" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True" />
        <Setter Property="Margin" Value="4,0,0,0" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Foreground" Value="{DynamicResource FluentNeutralForeground1Brush}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="Padding" Value="6,0,6,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TopPanelItem}">
                    <Border x:Name="Border" BorderThickness="0" SnapsToDevicePixels="True"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{DynamicResource FluentBorderRadiusMedium}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsToggled" Value="True">
                <Setter Property="Background" Value="{DynamicResource FluentSubtleBackgroundHoverBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource FluentCompoundBrandForeground1Brush}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource FluentSubtleBackgroundHoverBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource FluentCompoundBrandForeground1Brush}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="Content" Value="{x:Null}" />
                    <Condition Property="ContentTemplate" Value="{x:Null}" />
                </MultiTrigger.Conditions>
                <MultiTrigger.Setters>
                    <Setter Property="Background" Value="{DynamicResource FluentPaletteRedBackground3Brush}" />
                </MultiTrigger.Setters>
            </MultiTrigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
