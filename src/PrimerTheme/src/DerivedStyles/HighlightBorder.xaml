<!--
    Overlay on Playnite Default/DerivedStyles/HighlightBorder.xaml.
    Playnite templates this theme does not replace (ListView, RichTextBox, DatePicker, PathSelectionBox, ComboBoxList)
    still wrap their fields in this border. It becomes Primer's TextInput chrome: 1px control-borderColor-rest edge,
    borderRadius-medium, focus-outlineColor while focus is inside. Hosts that set their own Background keep it.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="HighlightBorder" TargetType="Border" BasedOn="{StaticResource {x:Type Border}}">
        <Setter Property="CornerRadius" Value="{DynamicResource PrimerBorderRadiusMedium}" />
        <Setter Property="BorderThickness" Value="{DynamicResource ControlBorderThickness}" />
        <Setter Property="BorderBrush" Value="{DynamicResource PrimerControlBorderColorRestBrush}" />
        <Setter Property="Background" Value="{DynamicResource PrimerBgColorDefaultBrush}" />
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource PrimerFocusOutlineColorBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
