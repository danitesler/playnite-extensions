<!--
    Overlay on Playnite Default/DefaultControls/ComboBox.xaml.
    The trigger is Primer's Select: TextInput chrome (bgColor-default fill, 1px control-borderColor-rest edge,
    borderRadius-medium, 32px, 12px inline padding) with the triangle-down octicon in fgColor-muted 4px from the end;
    keyboard focus draws the accent border plus its 1px inset shadow as a 2px focus-outlineColor edge. The list opens
    as an ActionMenu: the Overlay (overlay-bgColor, borderRadius-large, 1px overlay-borderColor edge) around an
    ActionList (8px padding, items inset 8px), items borderRadius-medium, 6px 8px, control-transparent-bgColor-hover
    under the pointer, and the check of a single-select list in the leading slot of the selected item.
    Part names (PART_EditableTextBox, PART_Popup) are Playnite's.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="{x:Type ComboBox}" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="Foreground" Value="{DynamicResource PrimerFgColorDefaultBrush}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Background" Value="{DynamicResource PrimerBgColorDefaultBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource PrimerControlBorderColorRestBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource ControlBorderThickness}" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Padding" Value="{DynamicResource PrimerTextInputPadding}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <Grid>
                        <Border x:Name="Border" SnapsToDevicePixels="True"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{DynamicResource PrimerBorderRadiusMedium}">
                            <Grid>
                                <ToggleButton x:Name="ToggleButton" Focusable="False" ClickMode="Press"
                                              IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                                            <DockPanel LastChildFill="False" Background="Transparent">
                                                <ContentPresenter DockPanel.Dock="Right" Margin="0,0,4,0" Width="16" Height="16"
                                                                  VerticalAlignment="Center"
                                                                  TextElement.Foreground="{DynamicResource PrimerFgColorMutedBrush}"
                                                                  Content="{DynamicResource PrimerOcticonTriangleDown}"
                                                                  ContentTemplate="{DynamicResource PrimerOcticonTemplate}" />
                                            </DockPanel>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <!-- Content inset by Padding (PrimerTextInputPadding) plus room for the indicator. -->
                                <Border Margin="0,0,12,0" Padding="{TemplateBinding Padding}" IsHitTestVisible="False">
                                    <ContentPresenter x:Name="ContentSite" HorizontalAlignment="Left" VerticalAlignment="Center"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" />
                                </Border>
                                <Border Margin="0,0,12,0" Padding="{TemplateBinding Padding}">
                                    <TextBox x:Name="PART_EditableTextBox" Visibility="Hidden" VerticalAlignment="Center"
                                             HorizontalAlignment="Stretch" Focusable="True"
                                             Style="{DynamicResource PrimerBareTextBox}"
                                             IsReadOnly="{TemplateBinding IsReadOnly}" />
                                </Border>
                                <Popup x:Name="PART_Popup" IsOpen="{TemplateBinding IsDropDownOpen}"
                                       Placement="Bottom" VerticalOffset="4" Focusable="False" AllowsTransparency="True">
                                    <Grid MaxHeight="{TemplateBinding MaxDropDownHeight}" MinWidth="{TemplateBinding ActualWidth}"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                                        <Border BorderBrush="{DynamicResource PopupBorderBrush}"
                                                BorderThickness="{DynamicResource PopupBorderThickness}"
                                                Background="{DynamicResource PopupBackgroundBrush}"
                                                CornerRadius="{DynamicResource PrimerBorderRadiusLarge}"
                                                Padding="{DynamicResource PrimerActionListPadding}">
                                            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                                                <ItemsPresenter Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained"
                                                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                            </ScrollViewer>
                                        </Border>
                                    </Grid>
                                </Popup>
                            </Grid>
                        </Border>
                        <Border x:Name="FocusRing" BorderThickness="2" Visibility="Collapsed"
                                IsHitTestVisible="False" SnapsToDevicePixels="True"
                                CornerRadius="{DynamicResource PrimerBorderRadiusMedium}"
                                BorderBrush="{DynamicResource PrimerFocusOutlineColorBrush}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter Property="Visibility" Value="Visible" TargetName="FocusRing" />
                        </Trigger>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter Property="IsTabStop" Value="False" />
                            <Setter Property="Visibility" Value="Visible" TargetName="PART_EditableTextBox" />
                            <Setter Property="Visibility" Value="Hidden" TargetName="ContentSite" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ComboBoxItem}">
        <Setter Property="Foreground" Value="{DynamicResource PrimerFgColorDefaultBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Padding" Value="{DynamicResource PrimerActionListItemPadding}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <Border x:Name="ItemBorder" Background="{TemplateBinding Background}"
                            CornerRadius="{DynamicResource PrimerBorderRadiusMedium}" Padding="{TemplateBinding Padding}">
                        <DockPanel>
                            <!-- Leading selection slot: 16px, 8px before the label. -->
                            <ContentPresenter x:Name="CheckIcon" DockPanel.Dock="Left" Visibility="Hidden"
                                              Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center"
                                              Content="{DynamicResource PrimerOcticonCheck}"
                                              ContentTemplate="{DynamicResource PrimerOcticonTemplate}" />
                            <ContentPresenter VerticalAlignment="Center" />
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Visibility" Value="Visible" TargetName="CheckIcon" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource PrimerControlTransparentBgColorHoverBrush}" TargetName="ItemBorder" />
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter Property="Background" Value="{DynamicResource PrimerControlTransparentBgColorHoverBrush}" TargetName="ItemBorder" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
