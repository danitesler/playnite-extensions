<!--
    Overlay on Playnite Default/DefaultControls/PasswordBox.xaml.
    Primer TextInput: bgColor-default fill, borderColor-default edge, radius medium; focus = accent border plus a
    1px inset accent shadow, drawn here as a 2px ring-colored edge over the control.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="{x:Type PasswordBox}">
        <Setter Property="KeyboardNavigation.TabNavigation" Value="None" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="FontSize" Value="{DynamicResource FontSize}" />
        <Setter Property="FontFamily" Value="{DynamicResource FontFamily}" />
        <Setter Property="CaretBrush" Value="{DynamicResource PrimerFgColorDefaultBrush}" />
        <Setter Property="SelectionBrush" Value="{DynamicResource PrimerBgColorAccentEmphasisBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource PrimerFgColorDefaultBrush}" />
        <Setter Property="Background" Value="{DynamicResource PrimerBgColorDefaultBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource PrimerControlBorderColorRestBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource ControlBorderThickness}" />
        <Setter Property="Padding" Value="{DynamicResource PrimerTextInputPadding}" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type PasswordBox}">
                    <Grid>
                        <Border x:Name="Border" SnapsToDevicePixels="True"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{DynamicResource PrimerBorderRadiusMedium}">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                        <Border x:Name="FocusRing" BorderThickness="2" Visibility="Collapsed"
                                IsHitTestVisible="False" SnapsToDevicePixels="True"
                                CornerRadius="{DynamicResource PrimerBorderRadiusMedium}"
                                BorderBrush="{DynamicResource PrimerFocusOutlineColorBrush}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter Property="BorderBrush" Value="{DynamicResource PrimerFocusOutlineColorBrush}" TargetName="Border" />
                            <Setter Property="Visibility" Value="Visible" TargetName="FocusRing" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
