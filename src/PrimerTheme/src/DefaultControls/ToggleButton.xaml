<!--
    Overlay on Playnite Default/DefaultControls/ToggleButton.xaml.
    Primer Button, default variant, as a pressed toggle: control-bgColor-rest fill, 1px control-borderColor-rest edge,
    borderRadius-medium, fontWeight medium, control-bgColor-hover on hover; pressed (aria-pressed) =
    control-bgColor-active, Primer's button-default-bgColor-selected.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="{x:Type ToggleButton}" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="Background" Value="{DynamicResource PrimerControlBgColorRestBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource PrimerControlFgColorRestBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource PrimerControlBorderColorRestBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource ControlBorderThickness}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Padding" Value="{DynamicResource PrimerButtonPadding}" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource PrimerFocusOutline}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border x:Name="Border" SnapsToDevicePixels="True"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{DynamicResource PrimerBorderRadiusMedium}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource PrimerControlBgColorHoverBrush}" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Background" Value="{DynamicResource PrimerControlBgColorActiveBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
