<!--
    Overlay on Playnite Default/Views/TopPanel.xaml.
    GitHub AppHeader: a 64px band in bgColor-inset with 16px padding and no bottom border. Like GitHub's global bar,
    the left side stays quiet and everything lives on the right: the search input (TextInput medium, 272px), then
    Playnite's view controls, filters and notifications as invisible 32px IconButtons with 16px Octicons.
    Notifications use GitHub's unread dot instead of a count; an active filter shows "Filter active" on the button.
    TopPanelMenu, the toggles and the icon templates keep Playnite's keys. All PART_ names are Playnite's.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <DataTemplate x:Key="TopPanelGeneralViewSettingsTemplate">
        <ContentPresenter Width="{DynamicResource PrimerOcticonSize}" Height="{DynamicResource PrimerOcticonSize}" Content="{DynamicResource PrimerOcticonViewSettings}" ContentTemplate="{DynamicResource PrimerOcticonTemplate}" />
    </DataTemplate>
    <DataTemplate x:Key="TopPanelFilterPresetsSelectionTemplate">
        <ContentPresenter Width="{DynamicResource PrimerOcticonSize}" Height="{DynamicResource PrimerOcticonSize}" Content="{DynamicResource PrimerOcticonFilterPresets}" ContentTemplate="{DynamicResource PrimerOcticonTemplate}" />
    </DataTemplate>
    <DataTemplate x:Key="TopPanelGroupSettingsTemplate">
        <ContentPresenter Width="{DynamicResource PrimerOcticonSize}" Height="{DynamicResource PrimerOcticonSize}" Content="{DynamicResource PrimerOcticonGroup}" ContentTemplate="{DynamicResource PrimerOcticonTemplate}" />
    </DataTemplate>
    <DataTemplate x:Key="TopPanelSortSettingsTemplate">
        <ContentPresenter Width="{DynamicResource PrimerOcticonSize}" Height="{DynamicResource PrimerOcticonSize}" Content="{DynamicResource PrimerOcticonSort}" ContentTemplate="{DynamicResource PrimerOcticonTemplate}" />
    </DataTemplate>
    <DataTemplate x:Key="TopPanelSwitchDetailsViewTemplate">
        <ContentPresenter Width="{DynamicResource PrimerOcticonSize}" Height="{DynamicResource PrimerOcticonSize}" Content="{DynamicResource PrimerOcticonDetailsView}" ContentTemplate="{DynamicResource PrimerOcticonTemplate}" />
    </DataTemplate>
    <DataTemplate x:Key="TopPanelSwitchGridViewTemplate">
        <ContentPresenter Width="{DynamicResource PrimerOcticonSize}" Height="{DynamicResource PrimerOcticonSize}" Content="{DynamicResource PrimerOcticonGridView}" ContentTemplate="{DynamicResource PrimerOcticonTemplate}" />
    </DataTemplate>
    <DataTemplate x:Key="TopPanelSwitchListViewTemplate">
        <ContentPresenter Width="{DynamicResource PrimerOcticonSize}" Height="{DynamicResource PrimerOcticonSize}" Content="{DynamicResource PrimerOcticonListView}" ContentTemplate="{DynamicResource PrimerOcticonTemplate}" />
    </DataTemplate>
    <DataTemplate x:Key="TopPanelUpdateButtonTemplate">
        <ContentPresenter Width="{DynamicResource PrimerOcticonSize}" Height="{DynamicResource PrimerOcticonSize}" TextElement.Foreground="{DynamicResource WarningBrush}" Content="{DynamicResource PrimerOcticonUpdate}" ContentTemplate="{DynamicResource PrimerOcticonTemplate}" />
    </DataTemplate>
    <DataTemplate x:Key="TopPanelExplorerSwitchTemplate">
        <ContentPresenter Width="{DynamicResource PrimerOcticonSize}" Height="{DynamicResource PrimerOcticonSize}" Content="{DynamicResource PrimerOcticonExplorer}" ContentTemplate="{DynamicResource PrimerOcticonTemplate}" />
    </DataTemplate>
    <DataTemplate x:Key="TopPanelSelectRandomGameButtonTemplate">
        <ContentPresenter Width="{DynamicResource PrimerOcticonSize}" Height="{DynamicResource PrimerOcticonSize}" Content="{DynamicResource PrimerOcticonRandom}" ContentTemplate="{DynamicResource PrimerOcticonTemplate}" />
    </DataTemplate>
    <DataTemplate x:Key="TopPanelViewSelectRandomGameButtonTemplate">
        <ContentPresenter Width="{DynamicResource PrimerOcticonSize}" Height="{DynamicResource PrimerOcticonSize}" Content="{DynamicResource PrimerOcticonViewRandom}" ContentTemplate="{DynamicResource PrimerOcticonTemplate}" />
    </DataTemplate>
    <DataTemplate x:Key="TopPanelSearchButtonTemplate">
        <ContentPresenter Width="{DynamicResource PrimerOcticonSize}" Height="{DynamicResource PrimerOcticonSize}" Content="{DynamicResource PrimerOcticonSearch}" ContentTemplate="{DynamicResource PrimerOcticonTemplate}" />
    </DataTemplate>

    <!-- Looked up from code for the main menu; the Primer ActionMenu of DefaultControls/ContextMenu.xaml. -->
    <Style x:Key="TopPanelMenu" TargetType="{x:Type ContextMenu}" BasedOn="{StaticResource {x:Type ContextMenu}}" />

    <!-- Invisible IconButton; selected fill while the panel is open or a filter applies. -->
    <Style x:Key="TopPanelFilterToggle" TargetType="CheckBox">
        <Setter Property="MinWidth" Value="32" />
        <Setter Property="Height" Value="32" />
        <Setter Property="Margin" Value="4,0,0,0" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Foreground" Value="{DynamicResource PrimerFgColorMutedBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Border Background="{TemplateBinding Background}" CornerRadius="{DynamicResource PrimerBorderRadiusMedium}"
                            Padding="8,0,8,0">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <ContentPresenter Width="{DynamicResource PrimerOcticonSize}" Height="{DynamicResource PrimerOcticonSize}" VerticalAlignment="Center" Content="{DynamicResource PrimerOcticonFilter}" ContentTemplate="{DynamicResource PrimerOcticonTemplate}" />
                            <TextBlock x:Name="TextFilter" Text="{DynamicResource LOCFilterActiveLabel}"
                                       Style="{DynamicResource BaseTextBlockStyle}" Foreground="{TemplateBinding Foreground}"
                                       Margin="8,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Tag" Value="{StaticResource True}">
                            <Setter Property="Visibility" Value="Visible" TargetName="TextFilter" />
                            <Setter Property="Background" Value="{DynamicResource PrimerControlTransparentBgColorHoverBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource PrimerFgColorDefaultBrush}" />
                        </Trigger>
                        <Trigger Property="Tag" Value="{StaticResource False}">
                            <Setter Property="Visibility" Value="Collapsed" TargetName="TextFilter" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Background" Value="{DynamicResource PrimerControlTransparentBgColorHoverBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource PrimerFgColorDefaultBrush}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource PrimerControlTransparentBgColorHoverBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource PrimerFgColorDefaultBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Invisible IconButton with GitHub's unread dot (accent, ringed in the header color); hidden when empty. -->
    <Style x:Key="TopPanelNotificationsToggle" TargetType="ToggleButton">
        <Setter Property="MinWidth" Value="32" />
        <Setter Property="Height" Value="32" />
        <Setter Property="Margin" Value="4,0,0,0" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource PrimerFgColorMutedBrush}" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Background="{TemplateBinding Background}" CornerRadius="{DynamicResource PrimerBorderRadiusMedium}">
                        <Grid>
                            <ContentPresenter Width="{DynamicResource PrimerOcticonSize}" Height="{DynamicResource PrimerOcticonSize}" Content="{DynamicResource PrimerOcticonNotifications}" ContentTemplate="{DynamicResource PrimerOcticonTemplate}" />
                            <Ellipse Width="10" Height="10" Margin="0,5,5,0"
                                     HorizontalAlignment="Right" VerticalAlignment="Top"
                                     Fill="{DynamicResource PrimerBgColorAccentEmphasisBrush}"
                                     Stroke="{DynamicResource PrimerBgColorInsetBrush}" StrokeThickness="2" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <DataTrigger Binding="{Api Notifications.Count}" Value="0">
                <Setter Property="Visibility" Value="Collapsed" />
            </DataTrigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Background" Value="{DynamicResource PrimerControlTransparentBgColorHoverBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource PrimerFgColorDefaultBrush}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource PrimerControlTransparentBgColorHoverBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource PrimerFgColorDefaultBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- GitHub's header search: a TextInput (medium, 32px) at its 272px minimum width. -->
    <Style x:Key="PrimerAppHeaderSearch" TargetType="{x:Type SearchBox}" BasedOn="{StaticResource {x:Type SearchBox}}">
        <Setter Property="Width" Value="272" />
        <Setter Property="Height" Value="32" />
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style TargetType="{x:Type TopPanel}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TopPanel}">
                    <!-- Right padding: the window buttons (14px + 3 x 32px + gaps) + 16px gap. -->
                    <Border Height="64" Padding="16,0,140,0" Background="{DynamicResource PrimerBgColorInsetBrush}">
                        <DockPanel>
                            <Border x:Name="PART_ElemMainMenu" Style="{DynamicResource PrimerAppHeaderMenuButton}"
                                    DockPanel.Dock="Left" Margin="0,0,8,0" VerticalAlignment="Center">
                                <ContentPresenter Width="16" Height="16" Content="{DynamicResource PrimerOcticonMainMenu}" ContentTemplate="{DynamicResource PrimerOcticonTemplate}" />
                            </Border>

                            <StackPanel x:Name="PART_PanelMainPluginItems" Orientation="Horizontal"
                                        VerticalAlignment="Center" Margin="4,0,0,0"
                                        DockPanel.Dock="{Settings PluginTopPanelAlignment}" />

                            <CheckBox x:Name="PART_ToggleNotifications" DockPanel.Dock="Right"
                                      Style="{DynamicResource TopPanelNotificationsToggle}" />

                            <CheckBox x:Name="PART_ToggleFilter" DockPanel.Dock="Right"
                                      ToolTip="{DynamicResource LOCFilters}"
                                      Style="{DynamicResource TopPanelFilterToggle}" />

                            <StackPanel x:Name="PART_PanelMainItems" Orientation="Horizontal" Margin="8,0,0,0"
                                        VerticalAlignment="Center" DockPanel.Dock="Right" />

                            <SearchBox x:Name="PART_TextMainSearch" DockPanel.Dock="Right"
                                       Style="{StaticResource PrimerAppHeaderSearch}"
                                       WindowChrome.IsHitTestVisibleInChrome="True" />

                            <!-- ProgressBar (default 8px, accent) with its label; left-aligned like GitHub's context area. -->
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center"
                                        Margin="8,0,16,0">
                                <StackPanel MaxWidth="480" MinWidth="240" VerticalAlignment="Center">
                                    <TextBlock Name="PART_TextProgressText" Margin="0,0,0,6"
                                               TextTrimming="CharacterEllipsis"
                                               FontSize="{DynamicResource FontSizeSmall}"
                                               Foreground="{DynamicResource PrimerFgColorMutedBrush}"
                                               Style="{DynamicResource BaseTextBlockStyle}" />
                                    <ProgressBar Name="PART_ProgressGlobal" Height="8" />
                                </StackPanel>
                                <Button Name="PART_ButtonProgressCancel"
                                        Content="{DynamicResource LOCCancelLabel}"
                                        WindowChrome.IsHitTestVisibleInChrome="True"
                                        Margin="12,0,0,0" />
                            </StackPanel>
                        </DockPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
